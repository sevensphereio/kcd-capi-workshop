update the helm multi cluster to use the one from volcamp
improve module-04 to create 3 clusters with helm
explain the applyonce / reconcile for clusterresourcesets and offer demos
update helm addon module to leverage clusters from module-04
update sveltos module to leverage clusters from module-04
add the deployment of the local-path-provisioner using CAAPH in the module-05
add more indepth explanation in each module so that each module is 100% fully self explanatory

module-01 improve the module to explain that clusterresourcesets are now GA since 1.10
improve the clusterctl commands to bind to the version 1.11.1
update all modules README.md links the relevant module name

module 3,5 challenge2 a revoir
module 5 how to interact directly with the deployed model without using curl?
module 3 how to use helm to deploy multiple clusters at once by changing a values?


add an explanation of why and what this command does for each commands in each modules.

rework key components in module-02 they are wrong
module 03 explain why we use labels 

add a wildcard cert for *.cern.xinity.cc
FQDN from ws1 to ws27

traefik LB 